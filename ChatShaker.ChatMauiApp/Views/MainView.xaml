<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:viewModels="clr-namespace:ChatShaker.ChatMauiApp.ViewModels"
             x:DataType="viewModels:MainPageViewModel"
             x:Class="ChatShaker.ChatMauiApp.Views.MainView">
    <Grid RowDefinitions="Auto,*">
        <ScrollView Orientation="Horizontal" Grid.Row="0" BackgroundColor="Aquamarine">
            <HorizontalStackLayout Padding="12" Spacing="10">
                <Button Text="My chats"
                        Command="{Binding NavigateRegionCommand}"
                        CommandParameter="ChatListPage"/>
                
                <Button Text="Send Invitation"
                        Command="{Binding NavigateRegionCommand}"
                        CommandParameter="SendInvitationPage"/>
                
                <Button Text="Recieved Invitations"
                        Command="{Binding NavigateRegionCommand}"
                        CommandParameter="RecievedInvitationPage"/>
            </HorizontalStackLayout>
        </ScrollView>
        
        <ContentView Grid.Row="1"
                     prism:RegionManager.RegionName="MainRegion"></ContentView>
    </Grid>
</ContentPage>